# Firebase Setup

1. Create Firebase projects for development and production.
2. Run `flutterfire configure` to generate `lib/firebase_options.dart`.
3. Update `.firebaserc` with real project aliases.
4. Deploy rules with `firebase deploy --only firestore,storage`.
