# Testing Strategy

- Unit tests cover domain logic, validators, mappers, and utilities.
- Widget tests cover reusable components and core app screens.
- Integration tests cover critical user journeys.
- Mocks and fakes belong in `test/mocks/`.
- Shared test setup belongs in `test/helpers/`.
