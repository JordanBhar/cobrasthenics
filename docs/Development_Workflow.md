# Development Workflow

1. Create or update a task in `docs/TASK_TRACKER.md`.
2. Implement inside the relevant feature folder.
3. Add or update tests in `test/unit/`, `test/widget/`, or `test/integration/`.
4. Run `flutter analyze` and `flutter test`.
5. Update docs when architecture, contracts, or setup changes.
