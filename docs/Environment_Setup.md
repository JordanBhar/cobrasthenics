# Environment Setup

Required tools:

- Flutter SDK
- Firebase CLI
- Node.js for Firebase Functions
- Melos for workspace commands, if the project grows into packages

Copy `.env.development` or `.env.production` and fill in environment-specific values locally.
