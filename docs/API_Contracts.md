# API Contracts

This document tracks external and internal data contracts.

## Firebase

- Firestore paths live in `lib/core/constants/firestore_paths.dart`.
- Storage paths live in `lib/core/constants/storage_paths.dart`.
- Security rules live in `firebase/firestore.rules` and `firebase/storage.rules`.
