name: cobrasthenics
description: Cobrasthenics — Calisthenics Training App
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  cloud_firestore: ^6.4.1
  dio: ^5.9.2
  firebase_auth: ^6.5.1
  firebase_core: ^4.9.0
  firebase_crashlytics: ^5.2.2
  flex_color_scheme: ^8.4.0
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2
  flutter_riverpod: ^3.3.1
  freezed_annotation: ^3.1.0
  go_router: ^17.2.3
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  json_annotation: ^4.12.0
  # State management — uncomment your preferred solution
  # flutter_riverpod: ^2.5.1
  # provider: ^6.1.2
  # flutter_bloc: ^8.1.5

  # Firebase — uncomment when ready
  # firebase_core: ^2.27.0
  # cloud_firestore: ^4.15.6
  # firebase_auth: ^4.18.0
  # firebase_storage: ^11.6.10

  # Navigation
  # go_router: ^14.0.0

  # Camera & ML (for form analysis screen)
  # camera: ^0.10.5
  # google_mlkit_pose_detection: ^0.10.0

  # Subscriptions
  # purchases_flutter: ^6.21.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.15.0
  freezed: ^3.2.5
  json_serializable: ^6.14.0

flutter:
  uses-material-design: true
  # Font declaration commented out - files not present
  # fonts:
  #   - family: DM Mono
  #     fonts:
  #       - asset: assets/fonts/DMMono-Regular.ttf
  #         weight: 400
  #       - asset: assets/fonts/DMMono-Medium.ttf
  #         weight: 500
