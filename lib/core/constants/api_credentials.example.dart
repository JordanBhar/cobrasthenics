/// Copy this file to `api_credentials.dart` and fill in local values.
/// is the safe, committed template. It shows the shape of the file and the names of the keys the app expects, but only contains placeholder values.
/// `api_credentials.dart` is intentionally ignored by Git. Never commit real
/// API keys, tokens, signing secrets, or private endpoints.
abstract class ApiCredentials {
  static const exerciseApiKey = 'replace-with-local-key';
  static const analyticsApiKey = 'replace-with-local-key';
  static const aiCoachApiKey = 'replace-with-local-key';
}
